{
  "name": "@trugamr/kit",
  "version": "0.0.0",
  "private": true,
  "description": "Shared runtime foundation for the Effect-native Servarr SDKs: HTTP request engine, config schemas, and schema helpers. Consumed from source — not built or published.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@effect/platform": "0.96.2",
    "effect": "3.21.4"
  }
}
