{
  "name": "@trugamr/kit",
  "version": "0.0.0",
  "description": "Shared runtime foundation for the Effect-native Servarr SDKs: HTTP request engine, config schemas, and schema helpers.",
  "files": [
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "default": "./dist/index.mjs"
    }
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsgo --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@effect/platform": "0.96.2",
    "effect": "3.21.4"
  }
}
