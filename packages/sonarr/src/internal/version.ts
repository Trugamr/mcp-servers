/**
 * The base path every request shares, e.g. `${apiBase}/series`. The single place
 * the `/api/v3` prefix is spelled; each resource module derives its own base from
 * it.
 */
export const apiBase = "/api/v3"
